<h1>Private Events</h1>
<ol>
  <% @events.each do | event | %>
    <li>
      <%= event.title %>
      <strong> Creador:</strong><%= event.creator.attributes %> | <%= event.creator.id %>
      <small><%= link_to "See event", event_path(event) %></small>
    </li>
  <% end %>
</ol>
